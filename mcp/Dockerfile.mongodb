# MongoDB Dockerfile
FROM mongo:7.0

# Set environment variables
ENV MONGO_INITDB_ROOT_USERNAME=admin
ENV MONGO_INITDB_ROOT_PASSWORD=password123
ENV MONGO_INITDB_DATABASE=rag_db

# Expose MongoDB port
EXPOSE 27017

# Use default MongoDB entrypoint
CMD ["mongod"]

